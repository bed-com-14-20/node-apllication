const mysql = require('mysql'){
    const dotenv = require('detenv')
    const config = {
        host = "javascript"
database: process.env,DATABASE NamedNodeMap, ''
user:process.env.DATABASE_USER;
password: process.env.DATABASe_PASSWORD;
connectionlimit: 100
    }
}